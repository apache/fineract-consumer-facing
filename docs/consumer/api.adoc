= API Reference

This section is the entry point to the BFF's HTTP API. It explains how to produce the OpenAPI specification, where to browse it interactively, and how the generated clients are built from it, and it closes with a summarized index of every endpoint. OpenAPI is a machine-readable JSON description of an HTTP API that captures every path, parameter, and response schema in one file. The BFF's spec is not written by hand: springdoc (a library that inspects the running Spring application) derives it from the controllers at runtime, so the spec always matches the code that is actually deployed.

For request and response schemas, use Swagger UI or the spec itself. This section deliberately stops at one line per endpoint.

== Generating the OpenAPI spec

The `openapi` Gradle task scrapes the spec from the running application at `http://localhost:8080/v3/api-docs` and writes it to `consumer/build/openapi/openapi.json`. Because the spec comes from the live application, the Docker stack must be up first. Run from `consumer/`:

[source,bash]
----
cd consumer
docker compose up -d --build --wait && ./gradlew openapi; docker compose down -v
----

Both databases keep their data on tmpfs (in-memory) mounts, so removing the containers on teardown is what gives the next run a clean database. The `-v` flag additionally removes any volumes created alongside the containers.

== Browsing Swagger UI

Swagger UI is a browser page that renders the OpenAPI spec as interactive documentation. You can expand each endpoint, see its schemas, and send test requests. The BFF serves it through springdoc's default paths (no custom paths are configured), and `SecurityConfig` permits them without authentication. With the stack up, on the BFF's host port 8080:

* Swagger UI: `http://localhost:8080/swagger-ui.html` (redirects to `/swagger-ui/index.html`)
* Raw spec, JSON: `http://localhost:8080/v3/api-docs`
* Raw spec, YAML: `http://localhost:8080/v3/api-docs.yaml`

The spec's title, version, and security scheme come from `OpenApiConfig` in `consumer.infrastructure.configs`. It declares the session cookie carrying the access token as the API-wide security scheme, so Swagger UI shows which endpoints expect an authenticated session.

== Generated clients

Two clients are generated from `consumer/build/openapi/openapi.json`, keeping the BFF contract the single source of truth for both the test harness and the frontend:

* `./gradlew generateJavaClient` produces a Java Feign client (Feign is a declarative HTTP client library) in `consumer/build/generated/java-client`. The Cucumber end-to-end test harness compiles against it.
* `./gradlew generateTypescriptClient` produces a TypeScript Angular client in `frontend/src/openapi-client`, imported by the frontend under the path alias `@bff/client`.

Always delete the old output directory first. The generator only adds and overwrites files, so services for endpoints that no longer exist would otherwise linger. Both directories are gitignored generated output, so nothing tracked by git is at risk. Run from `consumer/`:

[source,bash]
----
cd consumer

# Java client (Cucumber test harness)
rm -rf build/generated/java-client
docker compose up -d --build --wait && ./gradlew generateJavaClient; docker compose down -v

# TypeScript client (Angular app)
rm -rf ../frontend/src/openapi-client
docker compose up -d --build --wait && ./gradlew generateTypescriptClient; docker compose down -v

# Both at once
rm -rf build/generated/java-client ../frontend/src/openapi-client
docker compose up -d --build --wait && ./gradlew openapi generateClients; docker compose down -v
----

== Endpoint index

One row per endpoint, grouped by feature module. All paths sit under the `/api/v1` prefix and, unless noted, require an authenticated session. Authorization rules (attribute-based access control) are covered in the security chapter; schemas live in Swagger UI.

=== Registration

[cols="1,4,6",options="header"]
|===
|Method |Path |Purpose
|POST |`/api/v1/registration/submit` |Submit registration details and the government ID for account binding (unauthenticated)
|POST |`/api/v1/registration/otp/send` |Send a one-time passcode (OTP) to the registrant's email (unauthenticated)
|POST |`/api/v1/registration/otp/verify` |Verify the OTP and activate the newly registered user (unauthenticated)
|===

=== Authentication

[cols="1,4,6",options="header"]
|===
|Method |Path |Purpose
|POST |`/api/v1/authentication/login` |Check email and password; issues a two-factor OTP challenge (unauthenticated)
|POST |`/api/v1/authentication/2fa` |Verify the OTP and establish the session (sets the auth cookies) (unauthenticated)
|POST |`/api/v1/authentication/refresh` |Rotate the refresh token and mint a new access token (unauthenticated)
|POST |`/api/v1/authentication/logout` |End the session and revoke its tokens
|===

=== User

[cols="1,4,6",options="header"]
|===
|Method |Path |Purpose
|POST |`/api/v1/user/password/change/initiate` |Start a password change; issues a step-up OTP challenge
|POST |`/api/v1/user/password/change/confirm` |Confirm the password change with the OTP
|POST |`/api/v1/user/password/forgot` |Request a password-reset OTP for a forgotten password (unauthenticated)
|POST |`/api/v1/user/password/reset` |Reset the password with the emailed OTP (unauthenticated)
|GET |`/api/v1/user/profile` |Fetch the logged-in user's profile
|GET |`/api/v1/user/charges` |List charges levied on the user's client record
|GET |`/api/v1/user/image` |Fetch the user's profile picture (read-only)
|GET |`/api/v1/user/obligees` |List guarantee obligations (loans the user stands guarantor for)
|===

=== Summary

[cols="1,4,6",options="header"]
|===
|Method |Path |Purpose
|GET |`/api/v1/summary/accounts` |Aggregated view of the user's savings and loan accounts
|===

=== Savings

[cols="1,4,6",options="header"]
|===
|Method |Path |Purpose
|GET |`/api/v1/savings` |List the user's savings accounts
|GET |`/api/v1/savings/{savingsId}` |Fetch one savings account with balance details
|GET |`/api/v1/savings/{savingsId}/charges` |List charges on a savings account
|GET |`/api/v1/savings/{savingsId}/transactions` |Search a savings account's transactions (paginated, filterable)
|GET |`/api/v1/savings/{savingsId}/transactions/{transactionId}` |Fetch one savings transaction
|GET |`/api/v1/savings/template` |Reference data (products, currencies) for savings accounts
|===

=== Loans

[cols="1,4,6",options="header"]
|===
|Method |Path |Purpose
|POST |`/api/v1/loans` |Submit a loan application
|PUT |`/api/v1/loans/{loanId}` |Modify a pending loan application
|POST |`/api/v1/loans/{loanId}` |Withdraw a pending loan application
|GET |`/api/v1/loans` |List the user's loan accounts
|POST |`/api/v1/loans/schedule-preview` |Preview the repayment schedule for a prospective loan (read-only; POST carries the parameters)
|GET |`/api/v1/loans/{loanId}` |Fetch one loan with outstanding balance and schedule
|GET |`/api/v1/loans/{loanId}/transactions` |List a loan's transactions (paginated, filterable)
|GET |`/api/v1/loans/{loanId}/transactions/{transactionId}` |Fetch one loan transaction
|GET |`/api/v1/loans/{loanId}/charges` |List charges on a loan
|GET |`/api/v1/loans/{loanId}/charges/{chargeId}` |Fetch one loan charge
|GET |`/api/v1/loans/{loanId}/guarantors` |List a loan's guarantors
|GET |`/api/v1/loans/template` |Reference data (products, terms) for loan applications
|===

=== Transfers

[cols="1,4,6",options="header"]
|===
|Method |Path |Purpose
|POST |`/api/v1/transfers/initiate` |Start a transfer; issues a step-up OTP challenge
|POST |`/api/v1/transfers/confirm` |Confirm the transfer with the OTP and execute it
|GET |`/api/v1/transfers` |List the user's transfer history (paginated)
|===

=== Beneficiaries

[cols="1,4,6",options="header"]
|===
|Method |Path |Purpose
|POST |`/api/v1/beneficiaries/initiate` |Start adding a beneficiary; issues a step-up OTP challenge
|POST |`/api/v1/beneficiaries/confirm` |Confirm the addition with the OTP
|PUT |`/api/v1/beneficiaries/{publicId}/initiate` |Start updating a beneficiary; issues a step-up OTP challenge
|PUT |`/api/v1/beneficiaries/{publicId}/confirm` |Confirm the update with the OTP
|DELETE |`/api/v1/beneficiaries/{publicId}` |Delete a beneficiary
|GET |`/api/v1/beneficiaries` |List the user's beneficiaries
|===

=== Audit

[cols="1,4,6",options="header"]
|===
|Method |Path |Purpose
|POST |`/api/v1/audit/events` |Ingest a batch of client-side audit events from the frontend
|GET |`/api/v1/audit/events` |List the caller's own audit events, paginated (demo-only; registered only when `consumer.audit.query-enabled` is set, off by default)
|===

=== Open banking

Endpoints for third-party providers (TPPs, licensed external applications a customer authorizes to read their data) plus the user-facing consent management surface. The four TPP rows do not use the session cookie: they authenticate with a consent-scoped OAuth2 bearer token, issued by the token endpoint below and validated by a dedicated resource-server filter chain. The two consent-management rows use the normal authenticated session.

[cols="1,4,6",options="header"]
|===
|Method |Path |Purpose
|POST |`/api/v1/openbanking/account-access-consents` |TPP creates an account-access consent request
|GET |`/api/v1/openbanking/account-access-consents/{consentId}` |TPP reads a consent's status
|GET |`/api/v1/openbanking/accounts` |TPP lists the accounts covered by the consent
|GET |`/api/v1/openbanking/accounts/{accountId}/balances` |TPP reads a consented account's balances
|GET |`/api/v1/openbanking/consents` |Logged-in user lists their granted consents
|POST |`/api/v1/openbanking/consents/{consentId}/revoke` |Logged-in user revokes a consent
|===

The OAuth2 protocol endpoints backing the TPP flow (`/api/v1/oauth2/authorize`, `/api/v1/oauth2/token`, `/api/v1/oauth2/jwks`, and `/api/v1/oauth2/revoke`) are provided by Spring Authorization Server rather than by BFF controllers. Their paths are configured in `OAuth2AuthorizationServerConfig`, and they do not appear in the OpenAPI spec because springdoc only documents controller endpoints.
