[[step-up]]
= Step-Up Challenges

Sensitive actions require the user to re-prove control of their second factor immediately before the action executes, even with a valid session. The client's job is small. It calls the initiate endpoint, shows the OTP entry screen, and echoes the challenge back at confirm time. Everything that makes the mechanism safe (the token's binding to user, device, and exact action parameters, and the OTP hashing and attempt limits) happens on the BFF and is described in `docs/consumer/security/step-up.adoc`.

== The client-side shape of every flow

Each screen follows the same three states, tracked in a `step` signal:

. *Form.* The user fills in the action (amount, payee, new password). Submitting calls the feature's initiate endpoint through its store. The response is a challenge object: an opaque `stepUpToken` (opaque meaning the client never parses or inspects it; it is carried, not read), an `expiresAt`, and `sentTo`, the masked email the OTP went to.
. *OTP.* The store puts the challenge in a signal and the component switches to the shared `OtpComponent` (`shared/otp/otp.component.ts`), which shows the masked destination and collects a six-character code. Cancelling returns to the form; the abandoned challenge simply expires server side.
. *Confirm.* The component sends the confirm request containing the `stepUpToken` from the store signal, the entered OTP, and the full action parameters again. The BFF recomputes a hash of those parameters and rejects the confirm if anything changed since initiate. On success the store clears the challenge signal (`challenge.set(null)`) and the screen shows its done state.

The challenge lives only in that store signal, in memory: it is never written to localStorage or a cookie. Because the stores are root-scoped services, the signal survives in-app navigation; it is cleared on a successful confirm and lost on a full page reload. The token is short-lived (5 minutes) and useless as a session credential: the BFF's access-token decoder rejects it outright because it carries a `purpose` claim.

== Screens that use the pattern

[cols="2,3,3", options="header"]
|===
|Screen |Store / component |BFF endpoints (initiate → confirm)

|Transfer (`features/transfers/transfer-form.component.ts`)
|`TransfersStore`
|`POST /api/v1/transfers/initiate` → `POST /api/v1/transfers/confirm`

|Loan repayment (`features/loans/loan-repay.component.ts`)
|`TransfersStore` (reused; a repayment is a transfer with `toAccountType` = `LOAN`)
|Same transfer endpoints as above

|Beneficiary add (`features/beneficiaries/beneficiaries.component.ts`)
|`BeneficiariesStore`
|`POST /api/v1/beneficiaries/initiate` → `POST /api/v1/beneficiaries/confirm`

|Beneficiary update (same component)
|`BeneficiariesStore`
|`PUT /api/v1/beneficiaries/{publicId}/initiate` → `PUT /api/v1/beneficiaries/{publicId}/confirm`

|Password change (`features/settings/change-password.component.ts`)
|`SettingsStore`
|`POST /api/v1/user/password/change/initiate` → `POST /api/v1/user/password/change/confirm`
|===

Every initiate and confirm also passes the device fingerprint as a typed parameter (see xref:device-fingerprint[Device Fingerprint]); the BFF requires the confirm to come from the same device as the initiate.

There is no client-side registry of "actions that need step-up". A screen participates because its BFF endpoints demand it; the client cannot opt an action out.

== Idempotency on money movement

The two transfer-backed confirms (transfer, loan repayment) additionally send an `Idempotency-Key` header: a client-chosen UUID that lets the BFF recognize a retried confirm and execute the transfer only once. The component mints the key lazily with `crypto.randomUUID()` and reuses it if the same confirm is retried after a network failure. It resets the key whenever the form values change (a changed form is a new action, not a retry) and clears it on success. Beneficiary and password-change confirms carry no idempotency key; they are naturally safe to retry.

== Denials

A rejected confirm (wrong OTP, expired challenge, or tampered parameters) comes back as HTTP 401 with a feature-specific error code. Because the response carries a code, `errorInterceptor` does not mistake it for an expired session: no refresh is attempted, the session stays intact, and the user sees a translated toast. The component stays on the OTP step so the user can retry the code. Once the OTP's attempt limit or either expiry is hit, the confirm keeps failing; the user then backs out to the form and initiates again, which mints a fresh challenge.

The login screen (`features/auth/login.component.ts`) reuses `OtpComponent` for its 2FA step but is not step-up: it elevates nothing, it creates the session in the first place, and its challenge token is held in a private component field rather than a store. See xref:session[Session Model].
