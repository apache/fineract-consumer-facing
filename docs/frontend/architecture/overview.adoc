[[overview]]
= Architecture Overview

== What the frontend is

The frontend in `frontend/` is an Angular single-page application. It exists to exercise the BFF's endpoints end to end: every screen is a thin surface over one or more BFF API calls, and the app contains no business logic of its own. The BFF decides, and the frontend renders state and dispatches actions.

The frontend talks only to the BFF. It never calls Fineract Core directly; that boundary is the reason the project exists (see `docs/consumer/architecture/overview.adoc`). All HTTP calls go to relative `/api/...` paths, which are proxied to the BFF in every environment (see xref:bff-integration[BFF Integration]).

== Bootstrap chain

The application starts in four steps:

. `src/main.ts` calls `bootstrapApplication(App, appConfig)`, the standalone-application entry point. There is no `NgModule`.
. `src/app/app.config.ts` supplies the `appConfig` providers: the router (`provideRouter(routes)`), the HTTP client with its interceptor chain, Ionic (`provideIonicAngular`), the translation service (ngx-translate, loading `/i18n/*.json`), the generated client's `Configuration` (base path `''`), and a global `ErrorHandler` (`AuditErrorHandler`, which reports uncaught errors to the BFF audit endpoint). It also registers an app initializer that calls `AuthService.refresh()` once before the first screen renders, so a page reload silently restores the session from the BFF's httpOnly refresh cookie.
. `src/app/app.ts` defines the root `App` component, which is nothing but a `<router-outlet>` (the placeholder where the router inserts the current screen).
. The router then loads either a public screen (login, registration) or the authenticated shell.

== Routes

All routes are declared in `src/app/app.routes.ts`, and two rules hold across the whole table. Every screen is lazy-loaded via `loadComponent` or `loadChildren`, meaning its JavaScript is only downloaded when the route is first visited. Everything outside the login, registration, and forgot-password screens requires a logged-in session, enforced by `authGuard` (a check the router runs before activating a route, described at the end of this section): the consent screen carries the guard directly, and all other screens nest under a path-less shell route the guard protects as a whole.

[cols="2,3,3", options="header"]
|===
|Path |Component |Purpose

|`/` |redirects to `/login` (`pathMatch: 'full'`) |Entry redirect to the login screen
|`/login` |`features/auth/login.component.ts` |Login screen (password then OTP)
|`/register` |`features/registration/registration.component.ts` |Multi-step registration wizard
|`/forgot-password` |`features/auth/forgot-password.component.ts` |Password reset via emailed OTP
|`/consent` |`features/consent/consent.component.ts` (rendered outside the shell) |Open banking consent approval screen
|`` (shell) |`layout/shell.component.ts`; all rows below render inside it |Layout wrapper for all authenticated screens
|`/summary` |`features/summary/summary.component.ts` |Dashboard aggregating savings and loans
|`/savings` |`features/savings/savings.routes.ts` (`SAVINGS_ROUTES`) |Savings account list
|`/savings/:savingsId` |`savings-detail.component.ts` |Savings account detail: balance, charges, transactions
|`/savings/:savingsId/transactions/:transactionId` |`savings-transaction.component.ts` |Single savings transaction detail
|`/loans` |`features/loans/loans.routes.ts` (`LOANS_ROUTES`) |Loan account list
|`/loans/apply` |`loan-apply.component.ts` |Loan application form with schedule preview
|`/loans/:loanId` |`loans-detail.component.ts` |Loan detail: outstanding balance, schedule, charges, guarantors
|`/loans/:loanId/repay` |`loan-repay.component.ts` |Loan repayment form
|`/loans/:loanId/transactions/:transactionId` |`loans-transaction.component.ts` |Single loan transaction detail
|`/transfers` |`features/transfers/transfer.component.ts` |Transfer history list
|`/transfers/new` |`features/transfers/transfer-form.component.ts` |New transfer form with OTP step
|`/beneficiaries` |`features/beneficiaries/beneficiaries.component.ts` |Saved-payee (beneficiary) management
|`/profile` |`features/profile/profile.component.ts` |User profile, charges, profile picture
|`/settings` |`features/settings/settings.component.ts` |Password change and open banking consent management
|`/demo` |`features/demo/demo.component.ts` |Demo screen querying back client-side audit events
|===

== App shell

`src/app/layout/shell.component.ts` is the one hand-built layout for all authenticated screens. It renders:

* A header toolbar with the brand, a sidenav toggle, a language switcher, and a logout button.
* An Ionic split pane (`ion-split-pane`) whose side menu lists the eight navigation entries (summary, savings, loans, transfers, beneficiaries, profile, settings, demo) as `routerLink` items; the collapsed state is a plain component signal.
* An indeterminate `ion-progress-bar` shown while the router is navigating, driven by subscribing to `Router.events` (`NavigationStart` sets a `loading` signal, `NavigationEnd`/`NavigationCancel`/`NavigationError` clear it).
* A `<router-outlet>` for the active feature screen.

The shell is also where client-side audit events for navigation originate. Every `NavigationEnd` records a `NAVIGATION` event with the route template (parameter names, not values). A lookup table of sensitive route templates additionally records a `SENSITIVE_VIEW` event; for example, viewing a savings account records a `BALANCE` view. Logout records a `LOGOUT` event before calling `AuthService.logout()`.

== Directory layout

[cols="1,3", options="header"]
|===
|Path under `frontend/src/app/` |Contents

|`api/`
|Hand-written TypeScript types for BFF contracts the OpenAPI generator does not emit. One file: `consumer-api-error.ts`, the `ConsumerApiError` error envelope (`code`, `defaultMessage`) that the BFF returns on failures.

|`core/`
|Singletons wired via `app.config.ts`: `auth/` (`AuthService`, `device-fingerprint.ts`), `guards/` (`authGuard`), `interceptors/` (`authInterceptor`, `errorInterceptor`), `audit/` (audit batching service, PII scrubbing, global error handler), `i18n/` (translation helper), `notifications/` (toast helper).

|`features/`
|One folder per BFF domain, each lazy-loaded: `auth`, `registration`, `consent`, `summary`, `savings`, `loans`, `transfers`, `beneficiaries`, `profile`, `settings`, `demo`. Eight of them (`summary`, `savings`, `loans`, `transfers`, `beneficiaries`, `profile`, `settings`, `demo`) hold their components plus a signal store (see xref:state-management[State Management]). The exceptions are `auth` (session state lives in the core `AuthService`), `registration` (a stateless service), and `consent` (a single component with no store or service).

|`layout/`
|The single `shell.component.ts` described above.

|`shared/`
|Reusable presentation pieces: `otp/` (the OTP entry component used by every step-up flow), `ui/` (page header, status badge), `utils/` (account sorting, date helpers), `css/` (styles shared by two or more screens), and the language switcher.
|===

The generated BFF client lives outside `app/`, in `frontend/src/openapi-client/` (imported as `@bff/client`); see xref:bff-integration[BFF Integration].

== The authGuard

`src/app/core/guards/auth.guard.ts` is a functional guard (a `CanActivateFn`: a plain function the router calls before activating a route, instead of a guard class):

* It injects `AuthService` and returns `true` when `auth.isAuthenticated()` is set.
* Otherwise it returns `router.createUrlTree(['/login'])`, redirecting to the login screen.

`isAuthenticated` is a `computed` over the session-expiry signal that the app initializer populates by attempting a refresh at startup. Deep links into guarded routes therefore survive a page reload as long as the refresh cookie is valid. The guard is a UX convenience only: the BFF rejects unauthenticated API calls regardless of what the client renders.
